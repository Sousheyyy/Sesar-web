# Database
DATABASE_URL="your-database-url"
DIRECT_URL="your-direct-url"

# TikTok OAuth (Official API)
TIKTOK_CLIENT_KEY="awl1tpwtadmop90a"
TIKTOK_CLIENT_SECRET="your-tiktok-client-secret"
TIKTOK_REDIRECT_URI="https://sesarapp.com/api/tiktok/callback"

# Encryption Key for storing tokens securely (64-character hex string = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-64-character-hex-encryption-key-generate-with-crypto"

# NextAuth Configuration
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="your-nextauth-secret"

# RapidAPI TikTok API23 (Primary TikTok data provider — cheap, fast)
RAPIDAPI_KEY="your-rapidapi-key"
RAPIDAPI_HOST="tiktok-api23.p.rapidapi.com"
RAPIDAPI_TIMEOUT_MS="15000"

# Apify TikTok Integration (Fallback provider — expensive, reliable)
# Get your API token from https://console.apify.com/account/integrations
APIFY_API_TOKEN="apify_api_xxx"
APIFY_TIKTOK_SOUND_SCRAPER_ACTOR_ID="naqsZgh7DhGajnD5z"
APIFY_TIMEOUT_MS="30000"
